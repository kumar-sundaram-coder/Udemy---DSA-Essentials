// Paste the below code in the global area
static const int FAST_IO = []()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    return 0;
}();

// OR, paste the below in any function or main function...

ios_base::sync_with_stdio(false);
cin.tie(0);